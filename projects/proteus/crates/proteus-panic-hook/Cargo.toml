[package]
name = "proteus-panic-hook"
version = "0.1.0"
edition.workspace = true
publish = false
license.workspace = true
authors.workspace = true
description = "Shared panic-observability hook + counter for proteus-server and proteus-client binaries"

[dependencies]
tracing = "0.1"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "warn"
