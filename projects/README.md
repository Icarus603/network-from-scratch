# projects/ — 動手實作

從 Part 10「設計你自己的協議」開始才會用到。

每個子專案一個目錄，包含：

- `README.md`：這個專案在學什麼
- `SPEC.md`：協議規格（如適用）
- 程式碼 + 對應語言的 build/test 設定
- 子專案的 build/run/test 指令也要同步寫到 repo 根的 `CLAUDE.md`

預期會出現的子專案（按大綱順序）：

| 名稱 | Part | 描述 |
|---|---|---|
| `myproto-go/` | 10 | 自己設計的代理協議 MVP（Go） |
